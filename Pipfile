[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
ipython = "*"
flake8 = "*"

[packages]
##
# Discord API connections
# New disco-based
# gevent = "~= 1.3"
disco-py = {git = "https://github.com/b1naryth1ef/disco.git", editable = true, ref = "v0.0.13-rc.2"}
requests = {version = "*", extras = ["security"]}
ujson = "*"

##
# Actual my stuff
pyhamcrest = "*"
pyyaml = "*"

# Other

[requires]
python_version = "3.7"

[scripts]
app = "python -m clockwork_croc.app"
